DATABASE_URL=postgres://user:password@localhost:5432/curios
PORT=4000
ANTHROPIC_API_KEY=sk-ant-...
DOSSIER_MCP_URL=https://mcp.dossier.mattic.dev/mcp
DOSSIER_MCP_API_KEY=
DOSSIER_API_URL=https://api.dossier.mattic.dev
DOSSIER_API_KEY=
CHAT_MAX_MESSAGES=20

# Optional: path to a JSON file with seed content (profile/experience/education
# + Swedish translations). If unset, seed.ts uses a placeholder fallback.
# For the canonical shape, see curios-dev/seed/content.json in the private dev repo.
# CI injects this content via SEED_CONTENT_JSON instead — see deploy.yml.
SEED_CONTENT_PATH=
